"""Map styles for the Stage 2 projector.

Every style takes the same scene and returns SVG, so they can be swapped without
the game or the server knowing. maps/gallery.py renders them side by side.
"""
